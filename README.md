# python
---Contents---
## 🟢 **Level 1: Basic Python**

### 1. Introduction to Python

* What is Python, why use it?
* Installing Python & IDEs (VS Code, PyCharm)
* Writing and running your first Python program
* Python 2 vs 3 (brief)

### 2. Basic Syntax

* Variables and data types
* Keywords and identifiers
* Input/output (`input()`, `print()`)

### 3. Control Flow

* `if`, `elif`, `else`
* Comparison and logical operators
* `pass`, `continue`, `break`

### 4. Loops

* `for` loop and `range()`
* `while` loop
* Nested loops

### 5. Data Structures

* Lists
* Tuples
* Sets
* Dictionaries

### 6. Functions

* Defining and calling functions
* Arguments and return values
* Default, keyword, and variable-length arguments

### 7. Modules & Packages

* Importing standard modules
* Creating and using your own module
* The `__name__ == "__main__"` idiom

### 8. Basic Error Handling

* `try`, `except`
* `finally`, `else`
* Basic exceptions (`ZeroDivisionError`, `TypeError`, etc.)

### 9. File Handling

* Reading and writing files
* Modes: read, write, append
* File pointer functions (`seek()`, `tell()`)

### 10. Practice Projects

* Number guessing game
* Basic calculator
* To-do list CLI app

---

## 🟡 **Level 2: Intermediate Python**

### 1. Object-Oriented Programming

* Classes and objects
* `__init__`, `self`
* Inheritance
* Method overriding
* Class and static methods

### 2. Python Data Model

* `__str__`, `__repr__`
* `__len__`, `__getitem__`, etc.

### 3. Functional Programming

* `map()`, `filter()`, `reduce()`
* `lambda` expressions
* `zip()`, `enumerate()`

### 4. File & Directory Handling

* `os` and `shutil` modules
* Creating/deleting directories
* Path handling with `pathlib`

### 5. Error Handling (Deep Dive)

* Custom exceptions
* Raising exceptions
* Chained exceptions

### 6. Iterators and Generators

* `__iter__`, `__next__`
* Generator functions
* `yield` vs `return`

### 7. Decorators and Closures

* Nested functions
* Closures
* Writing decorators
* `@wraps`

### 8. Comprehensions

* List, set, dictionary comprehensions
* Conditional comprehensions
* Nested comprehensions

### 9. Working with External Libraries

* Using `pip`
* Libraries: `requests`, `datetime`, `random`
* Creating and using virtual environments

### 10. Practice Projects

* Expense Tracker
* Weather CLI app (using API)
* File Organizer

---

## 🔴 **Level 3: Advanced Python**

### 1. Advanced OOP

* Multiple inheritance and MRO
* `__slots__`
* Metaclasses
* Descriptors
* Abstract Base Classes (`abc`)

### 2. Concurrency

* Threading vs multiprocessing
* `threading` module
* `multiprocessing` module
* `concurrent.futures`
* Async programming with `asyncio`

### 3. Memory & Performance

* Python memory model
* Garbage collection
* Profiling (`cProfile`, `line_profiler`)
* Optimization techniques

### 4. Type Hints & Annotations

* Static typing with `mypy`
* Generics, Protocols, TypedDict
* `dataclasses` vs `attrs`

### 5. Advanced Decorators & Context Managers

* Parameterized decorators
* Writing reusable context managers
* `contextlib` utilities

### 6. Testing & Debugging

* `unittest`, `pytest`, `mock`
* TDD principles
* Debugging with `pdb`

### 7. Networking & APIs

* Working with `socket`
* HTTP requests with `requests`, `http.client`
* Consuming and building REST APIs (Flask/FastAPI)

### 8. Metaprogramming & Introspection

* `getattr`, `setattr`, `hasattr`
* `eval`, `exec`
* Modifying classes/functions at runtime

### 9. Packaging & Distribution

* Creating installable packages
* `setuptools`, `pyproject.toml`
* Publishing to PyPI

### 10. Capstone Projects

* Blog app with Flask/FastAPI
* Chat app using sockets
* Asynchronous web scraper
* CLI tool with decorators, API, and packaging

---

### 🔁 Optional Tracks After This:

* **Web Development** (Flask, Django, FastAPI)
* **Data Science & ML** (NumPy, Pandas, Scikit-learn, TensorFlow)
* **DevOps/Scripting** (Bash, Docker, Python automation)
